#Prompt user for bill amount
def check_split(bill):
    check_split = int(input('What is the bill amount? :/n'))
#Check user response and prompt again if needed
    n = 3
    while n > 0:
        if check_split >= 0
            party_total = int(input('How many people are in your Party? : /n'))
         n = n - 1
        elif print('please enter a postive bill amount.')
#Prompt user for a names of party and salaries
    name = party_total
        while True:
            name[] = int(input(Please enter Guest n name, end =''))
            n = n -1
    values = []
    for i in range(party_total):
        values.append(int(input('Please imput salaries for guest name []')))  
Return None
#Calculate total salary for party





#Calculate percentage of total salary for each party member

#For each member:

     #Calculate the portion of the bill based on their salary percentage

     #Print name and bill portion



def is_even(bunnies)
    if isinstance(bunnies,int)
        if bunnies % 2 == 0:
            return True
        else:
            return False
    else:
        print("number please")
        Return None

is_even(8)
is_even(5)
is_even("cow")        